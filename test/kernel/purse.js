/**
 * purse.js
 *
 * Purse API tests that should work across all purse implementations.
 */

const { describe, it } = require('mocha')

// ------------------------------------------------------------------------------------------------
// Purse tests
// ------------------------------------------------------------------------------------------------

describe('Purse', () => {
  describe('pay', () => {
    it('test', () => {
      console.log('hello')
    })
  })

  describe('broadcast', () => {
    it('test', () => {
      console.log('hello')
    })
  })
})

// ------------------------------------------------------------------------------------------------
