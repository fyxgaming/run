/**
 * code.js
 * 
 * A jig for code such as classes and functions
 */

const Sandbox = require('../util/sandbox')
const JAL = require('./jal')

class Code {
    constructor() {
        // TODO
    }
}

connew Code(f, options)

module.exports = Sandbox._instance._sandboxType(Code, { JAL })
